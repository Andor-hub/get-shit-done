---
phase: 04-notifications-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HabitX/HabitX/PrivacyInfo.xcprivacy
  - HabitX/HabitXWidget/PrivacyInfo.xcprivacy
  - project.yml
  - HabitX/HabitX/Features/Today/TodayView.swift
  - HabitX/HabitX/Features/History/HistoryListView.swift
  - HabitX/HabitX/Features/Stats/StatsListView.swift
  - .planning/phases/04-notifications-polish/04-REGRESSION.md
autonomous: true
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-04]

must_haves:
  truths:
    - "Both app and widget targets include PrivacyInfo.xcprivacy in Copy Bundle Resources"
    - "App Store Connect accepts the binary without privacy manifest warnings"
    - "Empty states show action-oriented copy when no habits exist"
    - "A regression checklist exists covering physical-device tests for Phases 3 and 4"
  artifacts:
    - path: "HabitX/HabitX/PrivacyInfo.xcprivacy"
      provides: "Privacy manifest for app target"
      contains: "NSPrivacyTracking"
    - path: "HabitX/HabitXWidget/PrivacyInfo.xcprivacy"
      provides: "Privacy manifest for widget extension target"
      contains: "NSPrivacyTracking"
    - path: ".planning/phases/04-notifications-polish/04-REGRESSION.md"
      provides: "TestFlight regression checklist"
      contains: "Physical Device"
  key_links:
    - from: "project.yml"
      to: "HabitX/HabitX/PrivacyInfo.xcprivacy"
      via: "sources directory auto-pickup"
      pattern: "HabitX/HabitX"
    - from: "project.yml"
      to: "HabitX/HabitXWidget/PrivacyInfo.xcprivacy"
      via: "sources directory auto-pickup"
      pattern: "HabitX/HabitXWidget"
---

<objective>
Complete App Store readiness: create PrivacyInfo.xcprivacy for both targets, verify empty state copy in all views, and produce the TestFlight regression checklist.

Purpose: Satisfies Phase 4 success criterion #4 (valid PrivacyInfo.xcprivacy + no crashes) and ensures first-time user experience is polished. The regression doc captures all physical-device tests deferred from Phase 3.
Output: Two PrivacyInfo.xcprivacy files, possibly updated empty state copy, project.yml fileTypes override, 04-REGRESSION.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-notifications-polish/04-CONTEXT.md
@.planning/phases/04-notifications-polish/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrivacyInfo.xcprivacy for both targets and configure project.yml</name>
  <files>
    HabitX/HabitX/PrivacyInfo.xcprivacy,
    HabitX/HabitXWidget/PrivacyInfo.xcprivacy,
    project.yml
  </files>
  <read_first>
    project.yml,
    .planning/phases/04-notifications-polish/04-RESEARCH.md
  </read_first>
  <action>
**1. Create `HabitX/HabitX/PrivacyInfo.xcprivacy`** (new file) per D-11.

Content (XML plist):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSPrivacyTracking</key>
    <false/>
    <key>NSPrivacyTrackingDomains</key>
    <array/>
    <key>NSPrivacyCollectedDataTypes</key>
    <array/>
    <key>NSPrivacyAccessedAPITypes</key>
    <array/>
</dict>
</plist>
```

**2. Create `HabitX/HabitXWidget/PrivacyInfo.xcprivacy`** (new file) — identical content to the app target file. Per RESEARCH Pattern 4, each target MUST have its own separate physical file to avoid "Multiple commands produce" XcodeGen build errors.

**3. Add `fileTypes` override to `project.yml`** to ensure XcodeGen places `.xcprivacy` files in Copy Bundle Resources (not Compile Sources). Per RESEARCH Pitfall 5, add under the existing `options:` key:

```yaml
options:
  fileTypes:
    xcprivacy:
      buildPhase: resources
```

This goes inside the existing `options:` block (which already has `bundleIdPrefix`, `deploymentTarget`, etc.). Do NOT create a duplicate `options:` key.

**4. Regenerate Xcode project:**
```bash
cd "/Users/ahedspeth/Library/Mobile Documents/com~apple~CloudDocs/Claude Code Workspace/HabitX 2.0" && xcodegen generate
```

**5. Verify** that both PrivacyInfo.xcprivacy files appear in Copy Bundle Resources for their respective targets by running the build.
  </action>
  <verify>
    <automated>cd "/Users/ahedspeth/Library/Mobile Documents/com~apple~CloudDocs/Claude Code Workspace/HabitX 2.0" && xcodegen generate && xcodebuild build -project HabitX.xcodeproj -scheme HabitX -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -derivedDataPath /tmp/habitx-build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `HabitX/HabitX/PrivacyInfo.xcprivacy` exists
    - File `HabitX/HabitXWidget/PrivacyInfo.xcprivacy` exists
    - grep `NSPrivacyTracking` HabitX/HabitX/PrivacyInfo.xcprivacy returns a match
    - grep `NSPrivacyTracking` HabitX/HabitXWidget/PrivacyInfo.xcprivacy returns a match
    - grep `xcprivacy` project.yml returns a match (fileTypes override)
    - grep `buildPhase: resources` project.yml returns a match
    - xcodegen generate succeeds
    - xcodebuild build succeeds (exit 0) with no errors
    - The two PrivacyInfo.xcprivacy files are NOT the same filesystem path (separate physical files)
  </acceptance_criteria>
  <done>
    Both targets have PrivacyInfo.xcprivacy declaring no tracking, no collected data, no API reasons. project.yml has fileTypes override for xcprivacy -> resources buildPhase. Project builds successfully after xcodegen regeneration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify empty state copy and create regression checklist</name>
  <files>
    HabitX/HabitX/Features/Today/TodayView.swift,
    HabitX/HabitX/Features/History/HistoryListView.swift,
    HabitX/HabitX/Features/Stats/StatsListView.swift,
    .planning/phases/04-notifications-polish/04-REGRESSION.md
  </files>
  <read_first>
    HabitX/HabitX/Features/Today/TodayView.swift,
    HabitX/HabitX/Features/History/HistoryListView.swift,
    HabitX/HabitX/Features/Stats/StatsListView.swift,
    .planning/phases/04-notifications-polish/04-CONTEXT.md
  </read_first>
  <action>
**1. Verify empty state copy** per D-12. Read each view and compare existing copy to spec:

- **TodayView**: Currently has `emptyStateView` with `Text("No habits yet")` and a tappable plus icon. D-12 spec: "No habits yet -- tap + to add your first one." The current copy is close but missing the action hint. Update the `Text` in `emptyStateView` to:
  ```swift
  Text("No habits yet -- tap + to add your first one")
  ```
  Only update if the current text does NOT already include the action hint. If it just says "No habits yet", update it.

- **HistoryListView**: Currently has `ContentUnavailableView("No Habits", systemImage: "clock", description: Text("Add habits from the Today tab to see history here."))`. D-12 spec: "No habits to show history for." The current copy is actually more helpful (directs user to Today tab). Keep the existing copy -- it matches D-12's intent and is more action-oriented. No change needed.

- **StatsListView**: Currently has `ContentUnavailableView("No Habits", systemImage: "chart.bar", description: Text("Add habits from the Today tab to see stats here."))`. D-12 spec: "No habits yet -- add one from the Today tab." The current copy already directs to the Today tab. Keep as-is -- matches intent. No change needed.

**2. Create `.planning/phases/04-notifications-polish/04-REGRESSION.md`** per D-14.

This is a manual TestFlight regression checklist document covering:

**Phase 3 deferred tests (physical device only):**
- [ ] Boolean habit widget: tap toggles done/undone, widget updates without opening app
- [ ] Count habit widget: tap increments by 1, widget updates without opening app
- [ ] Input habit widget: tap opens app to NumberInputSheet for that habit
- [ ] After widget interaction, open app -- Today view shows updated state (not stale)
- [ ] Deep-link via habitx:// URL opens correct habit's input sheet

**Phase 4 notification tests (physical device only):**
- [ ] Create habit, enable reminder, set time -- notification fires at set time
- [ ] Notification shows only habit name as title, no body text
- [ ] Disable reminder toggle and save -- no notification fires
- [ ] Enable reminder on first habit -- system permission dialog appears
- [ ] Deny notification permission -- toggle reverts, inline Settings nudge appears
- [ ] Complete a habit, bring app to foreground, then background -- pending notification for that habit is cancelled
- [ ] Verify notification does NOT fire for a habit already completed today

**General regression:**
- [ ] App launches without crash on physical device
- [ ] All three habit types (boolean, count, input) can be created and logged
- [ ] History and Stats views show correct data
- [ ] Empty states display correct copy when no habits exist

Include a header noting this is for TestFlight builds on physical devices, not Simulator.
  </action>
  <verify>
    <automated>cd "/Users/ahedspeth/Library/Mobile Documents/com~apple~CloudDocs/Claude Code Workspace/HabitX 2.0" && test -f .planning/phases/04-notifications-polish/04-REGRESSION.md && echo "REGRESSION.md exists" && grep -c "\\[ \\]" .planning/phases/04-notifications-polish/04-REGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-notifications-polish/04-REGRESSION.md` exists
    - grep `Physical Device` 04-REGRESSION.md returns a match (or similar header text)
    - grep `widget` 04-REGRESSION.md returns matches (Phase 3 deferred tests)
    - grep `notification` 04-REGRESSION.md returns matches (Phase 4 tests)
    - grep `permission` 04-REGRESSION.md returns a match
    - The checklist has at least 14 items (checkboxes)
    - TodayView empty state copy includes action hint (tap + to add)
    - xcodebuild build still succeeds after any view changes
  </acceptance_criteria>
  <done>
    Empty state copy verified and updated where needed. 04-REGRESSION.md contains a comprehensive physical-device TestFlight regression checklist covering Phase 3 widget interactions, Phase 4 notification flows, and general app regression. At least 14 checkable test steps exist.
  </done>
</task>

</tasks>

<verification>
1. Both PrivacyInfo.xcprivacy files exist in their respective target directories
2. project.yml has fileTypes xcprivacy -> resources override
3. xcodegen generate + xcodebuild build succeeds with no errors
4. Empty state copy is action-oriented in all three views
5. 04-REGRESSION.md exists with at least 14 physical-device test steps
</verification>

<success_criteria>
- PrivacyInfo.xcprivacy present in both HabitX app and HabitXWidget targets
- Privacy manifest declares no tracking, no data collection, no API reasons
- XcodeGen correctly places xcprivacy files in Copy Bundle Resources
- Empty states guide users to add habits (action-oriented copy)
- Regression checklist covers all Phase 3 deferred tests and Phase 4 notification tests
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-notifications-polish/04-02-SUMMARY.md`
</output>
