---
phase: 02-core-app
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - HabitX/HabitX/Features/Root/TabRootView.swift
  - HabitX/HabitX/Features/Today/TodayView.swift
  - HabitX/HabitX/Features/Today/HabitCardView.swift
  - HabitX/HabitX/Features/Today/NumberInputSheet.swift
  - HabitX/HabitX/Features/HabitForm/HabitTemplatePickerView.swift
  - HabitX/HabitX/Features/HabitForm/HabitFormView.swift
  - HabitX/HabitX/HabitXApp.swift
  - HabitX/HabitX/ContentView.swift
autonomous: true
requirements:
  - HAB-01
  - HAB-03
  - HAB-04
  - HAB-05
  - TODAY-01
  - TODAY-02
  - TODAY-03
  - TODAY-04

must_haves:
  truths:
    - "User sees all habits as cards in a scrollable Today view sorted by sortOrder"
    - "Each card shows habit name and current progress vs daily target (e.g. 'Water -- 3/8 cups')"
    - "Completed habits are visually distinct from incomplete habits (accent fill vs muted)"
    - "Tapping anywhere on a boolean card toggles done/undone"
    - "Tapping + on a count card increments by 1 immediately"
    - "Tapping + on an input card opens a number input sheet for value entry"
    - "User can create a habit via Add button -- picker offers Protein/Water templates or blank custom habit"
    - "User can edit a habit's name, type, target, and unit via edit sheet"
    - "User can delete a habit with confirmation dialog"
    - "User can reorder habits via drag-to-reorder in edit mode"
    - "Tab bar shows Today, History, and Stats tabs"
  artifacts:
    - path: "HabitX/HabitX/Features/Root/TabRootView.swift"
      provides: "TabView with Today/History/Stats tabs"
      exports: ["TabRootView"]
    - path: "HabitX/HabitX/Features/Today/TodayView.swift"
      provides: "Main Today tab with @Query habits, edit mode, add button, empty state"
      exports: ["TodayView"]
    - path: "HabitX/HabitX/Features/Today/HabitCardView.swift"
      provides: "Single habit card with type-dispatched logging interaction"
      exports: ["HabitCardView"]
    - path: "HabitX/HabitX/Features/Today/NumberInputSheet.swift"
      provides: "Sheet for input-type habit value entry with number pad"
      exports: ["NumberInputSheet"]
    - path: "HabitX/HabitX/Features/HabitForm/HabitTemplatePickerView.swift"
      provides: "Template picker: Protein, Water, or Custom"
      exports: ["HabitTemplatePickerView"]
    - path: "HabitX/HabitX/Features/HabitForm/HabitFormView.swift"
      provides: "Reusable add/edit form for habit configuration"
      exports: ["HabitFormView"]
  key_links:
    - from: "HabitX/HabitX/Features/Today/TodayView.swift"
      to: "HabitX/HabitX/Features/Today/HabitCardView.swift"
      via: "ForEach(habits) rendering HabitCardView for each habit"
      pattern: "ForEach.*habits.*HabitCardView"
    - from: "HabitX/HabitX/Features/Today/HabitCardView.swift"
      to: "HabitX/HabitX/Services/HabitLogService.swift"
      via: "Tap/button actions calling toggleBoolean/incrementCount/setValue"
      pattern: "HabitLogService\\.(toggleBoolean|incrementCount|setValue)"
    - from: "HabitX/HabitX/Features/HabitForm/HabitTemplatePickerView.swift"
      to: "HabitX/HabitX/Utilities/HabitDefaults.swift"
      via: "Reading defaultHabits templates for picker options"
      pattern: "HabitDefaults\\.defaultHabits"
    - from: "HabitX/HabitX/HabitXApp.swift"
      to: "HabitX/HabitX/Features/Root/TabRootView.swift"
      via: "App entry point renders TabRootView instead of ContentView"
      pattern: "TabRootView\\(\\)"
---

<objective>
Build the Today view, habit management flows, and tab bar shell -- the primary user-facing surface of HabitX. After this plan, users can create/edit/delete/reorder habits, log all three habit types from cards, and navigate between Today/History/Stats tabs.

Purpose: This is the core interaction layer. Every requirement about habit management (HAB-01/03/04/05) and today's view (TODAY-01/02/03/04) is addressed here. History and Stats tabs get placeholder content that plans 02-03 and 02-04 will fill in.
Output: TabRootView, TodayView, HabitCardView, NumberInputSheet, HabitTemplatePickerView, HabitFormView, updated HabitXApp.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-app/02-RESEARCH.md
@.planning/phases/02-core-app/02-CONTEXT.md
@.planning/phases/02-core-app/02-01-SUMMARY.md

@HabitX/HabitX/Models/Schema/HabitSchemaV1.swift
@HabitX/HabitX/Models/SharedModelContainer.swift
@HabitX/HabitX/HabitXApp.swift
@HabitX/HabitX/ContentView.swift
@project.yml

<interfaces>
<!-- From plan 02-01 outputs (service layer this plan depends on) -->

From HabitX/HabitX/Services/HabitLogService.swift:
```swift
enum HabitLogService {
    @MainActor static func toggleBoolean(habit: HabitSchemaV1.Habit, context: ModelContext)
    @MainActor static func incrementCount(habit: HabitSchemaV1.Habit, context: ModelContext)
    @MainActor static func setValue(habit: HabitSchemaV1.Habit, value: Double, context: ModelContext)
    static func todayValue(for habit: HabitSchemaV1.Habit) -> Double
    static func isCompleted(habit: HabitSchemaV1.Habit) -> Bool
}
```

From HabitX/HabitX/Utilities/HabitDefaults.swift:
```swift
struct HabitTemplate: Sendable {
    let name: String
    let habitType: HabitType
    let dailyTarget: Double
    let unit: String
}

enum HabitDefaults {
    static let proteinTemplate: HabitTemplate
    static let waterTemplate: HabitTemplate
    static let defaultHabits: [HabitTemplate]
    static func createHabit(from template: HabitTemplate, sortOrder: Int) -> HabitSchemaV1.Habit
    static let appAccentColor: Color  // Calm teal
}
```

From HabitX/HabitX/Models/Schema/HabitSchemaV1.swift:
```swift
@Model final class Habit {
    var id: UUID, var name: String, var habitType: String,
    var dailyTarget: Double, var unit: String, var sortOrder: Int,
    var reminderTime: Date?, var createdAt: Date
    @Relationship(deleteRule: .cascade, inverse: \HabitLog.habit) var logs: [HabitLog]
}
enum HabitType: String, CaseIterable, Sendable { case boolean, count, input }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tab bar shell, TodayView with habit cards and logging interactions</name>
  <files>
    HabitX/HabitX/Features/Root/TabRootView.swift,
    HabitX/HabitX/Features/Today/TodayView.swift,
    HabitX/HabitX/Features/Today/HabitCardView.swift,
    HabitX/HabitX/Features/Today/NumberInputSheet.swift,
    HabitX/HabitX/HabitXApp.swift
  </files>
  <behavior>
    - TabRootView renders a TabView with 3 tabs: Today (house icon), History (clock icon), Stats (chart.bar icon)
    - History and Stats tabs show placeholder Text views ("History" / "Stats") -- plans 02-03 and 02-04 replace them
    - TodayView uses @Query(sort: \HabitSchemaV1.Habit.sortOrder) for live habit list
    - TodayView shows NavigationStack with title "Today" and toolbar Edit button + Add (+) button
    - When habits list is empty, show centered "Add Habit" button as empty state (per D-03)
    - TodayView uses List (not ScrollView) for native onMove/onDelete support (per user decision: List for drag-to-reorder)
    - Each habit renders as HabitCardView inside the List
    - HabitCardView: rounded card shape, shows habit name and progress text (per D-01, D-02)
      - Boolean: shows "Done" or "Not done", tap anywhere on card calls HabitLogService.toggleBoolean (per D-04)
      - Count: shows "3/8 cups", has + button that calls HabitLogService.incrementCount (per D-05)
      - Input: shows "35/150 g", has + button that presents NumberInputSheet (per D-06)
    - Completed habits (isCompleted == true) get accent color fill; incomplete get secondary/muted style (per TODAY-03)
    - NumberInputSheet: sheet with TextField(.decimalPad), habit name as title, "Log" confirm button that calls HabitLogService.setValue and dismisses
    - HabitXApp.swift: replace ContentView() with TabRootView()
    - Edit mode: toolbar EditButton, List .onMove updates sortOrder on all affected habits (per D-09, HAB-05), .onDelete triggers deletion with confirmationDialog (per HAB-04)
  </behavior>
  <action>
    Create directory structure: Features/Root/, Features/Today/.

    1. **TabRootView.swift**: Simple TabView with 3 Tab items. Use Label("Today", systemImage: "house") etc. Today tab shows TodayView(), History tab shows placeholder Text("History — Coming Soon"), Stats tab shows placeholder Text("Stats — Coming Soon"). Apply .tint(HabitDefaults.appAccentColor) to the TabView for accent theming (per D-12).

    2. **TodayView.swift**: Use @Query(sort: \HabitSchemaV1.Habit.sortOrder) to load habits. @Environment(\.modelContext) for writes. @State properties for: showingAddSheet (Bool), habitToEdit (HabitSchemaV1.Habit?), habitToDelete (HabitSchemaV1.Habit?), showDeleteConfirmation (Bool). Wrap in NavigationStack. When habits.isEmpty, show a centered VStack with a large "Add Habit" Button styled as .borderedProminent with accent color. When habits exist, use List with ForEach rendering HabitCardView. Attach .onMove modifier that reindexes sortOrder: after move, iterate habits array and set habit.sortOrder = index for each. Attach .onDelete that sets habitToDelete and showDeleteConfirmation. Add .confirmationDialog for delete confirmation with "Delete \(habit.name)?" — on confirm, call modelContext.delete(habit). Toolbar: leading EditButton(), trailing Button with plus.circle systemImage that sets showingAddSheet = true. Sheet for showingAddSheet presents HabitTemplatePickerView. Sheet for habitToEdit presents HabitFormView in edit mode.

    3. **HabitCardView.swift**: Takes a HabitSchemaV1.Habit parameter. Compute todayValue via HabitLogService.todayValue(for:) and isCompleted via HabitLogService.isCompleted(habit:). Use @Environment(\.modelContext). Parse habitType via HabitType(rawValue: habit.habitType). Layout: RoundedRectangle card with padding. Top line: habit.name in .headline font. Second line: progress text formatted per type:
       - boolean: "Done" (checkmark) or "Not done"
       - count: "\(Int(todayValue))/\(Int(habit.dailyTarget)) \(habit.unit)" (e.g. "3/8 cups")
       - input: "\(todayValue, specifier: "%.0f")/\(habit.dailyTarget, specifier: "%.0f") \(habit.unit)" (e.g. "35/150 g")
       Background: if completed, HabitDefaults.appAccentColor.opacity(0.15) with accent-colored text; if incomplete, Color(.secondarySystemBackground).
       Interaction: For boolean, attach .onTapGesture { HabitLogService.toggleBoolean(habit:context:) } to the entire card. For count and input, show a + Button (circle with plus icon) on the trailing edge. Count + button calls HabitLogService.incrementCount directly. Input + button sets @State showingNumberInput = true. Pass showingNumberInput to sheet presenting NumberInputSheet.
       In edit mode (detect via @Environment(\.editMode)): tapping a card opens the edit sheet instead of logging. Bind habitToEdit on the parent TodayView via a closure callback or use a Binding.

    4. **NumberInputSheet.swift**: Takes habit: HabitSchemaV1.Habit binding/parameter. @State var inputValue: String = "". @Environment(\.dismiss) and @Environment(\.modelContext). NavigationStack with title "\(habit.name)". TextField("Value", text: $inputValue).keyboardType(.decimalPad). "Log" Button: parses Double from inputValue, calls HabitLogService.setValue(habit:value:context:), dismisses. Disable Log button when inputValue is empty or not a valid number.

    5. **HabitXApp.swift**: Change ContentView() to TabRootView(). No other changes.

    Use SwiftUI adaptive colors throughout — no forced light/dark (per D-11). Use HabitDefaults.appAccentColor for all accent styling (per D-12).
  </action>
  <verify>
    <automated>cd HabitX && xcodebuild build -target HabitX -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>App launches with 3-tab interface. Today view shows habit cards with correct progress text per type. Boolean tap toggles. Count + increments. Input + opens number sheet. Empty state shows Add button. Edit mode enables reorder and delete with confirmation. All styling uses accent color and respects system appearance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add habit flow (template picker + form) and edit habit sheet</name>
  <files>
    HabitX/HabitX/Features/HabitForm/HabitTemplatePickerView.swift,
    HabitX/HabitX/Features/HabitForm/HabitFormView.swift
  </files>
  <behavior>
    - HabitTemplatePickerView shows a sheet with options: Protein template, Water template, and "Custom Habit" (per D-03)
    - Selecting a template creates a habit from HabitDefaults.createHabit and opens HabitFormView pre-filled so user can adjust (per HAB-02)
    - Selecting "Custom Habit" opens HabitFormView with blank fields (per HAB-01)
    - HabitFormView works for both add (new habit) and edit (existing habit) modes (per HAB-03)
    - Form fields: name (TextField), type (Picker with HabitType.allCases), daily target (TextField .decimalPad), unit (TextField)
    - Save button: for add mode, inserts new Habit into modelContext; for edit mode, updates existing habit properties
    - Cancel button dismisses without saving
  </behavior>
  <action>
    Create directory: Features/HabitForm/.

    1. **HabitTemplatePickerView.swift**: A sheet view. @Environment(\.modelContext) and @Environment(\.dismiss). @State var selectedTemplate: HabitTemplate? = nil, @State var showingForm = false, @State var newHabit: HabitSchemaV1.Habit? = nil. Receives habitCount: Int (to set sortOrder = habitCount on new habits). NavigationStack with title "Add Habit". List with 3 rows:
       - "Protein" row with subtitle "150g daily target, input type" — on tap: create habit from HabitDefaults.proteinTemplate via createHabit(from:sortOrder:), set as newHabit, show form
       - "Water" row with subtitle "8 cups daily target, count type" — on tap: same with waterTemplate
       - "Custom Habit" row with subtitle "Start from scratch" — on tap: create a blank HabitSchemaV1.Habit() with sortOrder set, show form
       When showingForm: present HabitFormView(habit: newHabit!, isNew: true) in a sheet or NavigationLink push. On form save, insert habit into modelContext and dismiss both sheets. On form cancel, discard the habit.

    2. **HabitFormView.swift**: Takes habit: HabitSchemaV1.Habit (the model to edit) and isNew: Bool. Use @Bindable var habit for two-way binding to model properties (per research Pattern: @Bindable on @Model). @Environment(\.dismiss) and @Environment(\.modelContext). Form layout:
       - Section "Details": TextField("Name", text: $habit.name), Picker("Type", selection: binding to HabitType) that writes back to habit.habitType as rawValue string
       - Section "Target": TextField for dailyTarget (bind to a @State string, parse to Double on save), TextField("Unit", text: $habit.unit) with placeholder like "cups", "g", etc.
       - Note: reminderTime editing is Phase 4 (NOTF-01) — do not include here
       Toolbar: leading Cancel button (dismiss without saving if isNew — delete the unsaved habit from context or simply don't insert it). Trailing Save button: validates name is not empty, for isNew inserts habit into modelContext, dismisses.

       IMPORTANT for edit mode: When isNew is false, @Bindable writes directly to the existing model — changes are auto-persisted by SwiftData. The Save button just dismisses. Cancel in edit mode should revert changes — use modelContext.rollback() or re-fetch. Simplest approach: for edit mode, copy values to @State on appear, write back on save.

       Actually, the cleanest pattern: For NEW habits, don't insert into context until Save is tapped. Build the habit in memory, populate fields via @Bindable, insert on save. For EDIT habits, @Bindable writes to the live model — changes persist immediately. Add a note that this is the standard SwiftData edit pattern.
  </action>
  <verify>
    <automated>cd HabitX && xcodebuild build -target HabitX -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Add button opens template picker with Protein, Water, and Custom options. Selecting a template pre-fills the form. Custom opens a blank form. Form has name, type picker, target, and unit fields. Save creates the habit (add) or updates it (edit). Cancel discards changes. All three habit types can be created and edited.</done>
</task>

</tasks>

<verification>
1. `xcodebuild build` succeeds for HabitX target with no errors
2. App launches in simulator with 3-tab bar (Today / History / Stats)
3. Empty state shows "Add Habit" button
4. Add flow: template picker offers Protein, Water, Custom; form saves correctly
5. Boolean card: tap toggles done/undone with visual change
6. Count card: + button increments value, progress text updates
7. Input card: + button opens number sheet, value logs on confirm
8. Edit mode: drag to reorder updates sortOrder, swipe-to-delete shows confirmation
9. Edit habit sheet: form pre-fills with existing values, save persists changes
</verification>

<success_criteria>
- Tab bar with Today/History/Stats is the app root (History and Stats show placeholder content)
- Today view displays all habits as cards sorted by sortOrder with name + progress (per D-01, D-02)
- All three logging interactions work: boolean tap, count increment, input sheet (per D-04, D-05, D-06)
- Completed habits visually distinct via accent color fill (per TODAY-03)
- Habit CRUD: create from templates or custom, edit fields, delete with confirmation, reorder (per HAB-01, HAB-03, HAB-04, HAB-05)
- Empty state is a single Add Habit button that opens template picker (per D-03)
- Uses @Query in views, HabitLogService for writes, system dark/light mode (per D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-app/02-02-SUMMARY.md`
</output>
